# Copy to .env and fill in values

# Public URL of the API (used by SvelteKit frontend)
PUBLIC_API_URL=http://localhost/api

# Directory where job files are stored (input + output, auto-cleaned)
# Default: Docker-managed volume. Override with a path on the host:
# ANON_JOBS_DIR=/home/youruser/anon-jobs
